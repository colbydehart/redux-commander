{
  "name": "redux-commander",
  "version": "0.0.9",
  "description": "Simple, Declarative side effect bindings for redux",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build:clean && npm run build:lib",
    "build:clean": "rimraf dist",
    "build:lib": "tsc -p tsconfig.json --pretty",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^22.4.2",
    "rimraf": "^2.6.2",
    "typescript": "^3.0.3"
  },
  "author": "",
  "homepage": "",
  "license": "MIT",
  "repository": "https://github.com/colbydehart/redux-commander",
  "peerDependencies": {
    "redux": "^4.0.0"
  }
}
