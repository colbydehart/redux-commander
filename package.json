{
  "name": "redux-commander",
  "version": "0.0.1",
  "description": "Simple, Declarative side effect bindings for redux",
  "main": "dist/index.js",
  "files": ["dist"],
  "scripts": {
    "build": "npm run build:clean && npm run build:lib && npm run build:flow",
    "build:lib": "babel -s inline -d dist src/",
    "build:clean": "rimraf dist",
    "build:flow": "flow-copy-source -v src dist",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-jest": "^22.1.0",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-preset-env": "^1.6.1",
    "flow-bin": "^0.66.0",
    "flow-copy-source": "^1.3.0",
    "jest": "^22.4.2",
    "regenerator-runtime": "^0.11.1",
    "rimraf": "^2.6.2"
  },
  "author": "",
  "homepage": "",
  "license": "MIT",
  "repository": "",
  "peerDependencies": {
    "redux": "^3.7.2"
  }
}
